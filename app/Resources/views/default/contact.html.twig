{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
    <div id="wrapper">
        <div id="container">
            <h1>Contact</h1>

            {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="送信" class="btn btn-primary" />
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

